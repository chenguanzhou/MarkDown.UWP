<Page
    x:Class="MarkDown.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarkDown.UWP"
    xmlns:controls="using:Cimbalino.Toolkit.Controls"
    xmlns:vm="using:MarkDown.UWP.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Main,Source={StaticResource Locator}}"
    mc:Ignorable="d">
    
    <!--<Page.BottomAppBar>
        <AppBar IsSticky="True" IsOpen="True">
            <AppBarButton Name="ButtonOpen" Icon="OpenLocal" Label="Open" Click="ButtonOpen_Click"></AppBarButton>
        </AppBar>
    </Page.BottomAppBar>-->

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <local:SourceEditor Grid.Column="0" CodeContent="{Binding Content,Mode=TwoWay}" />

            <local:Preview Grid.Column="1" PreviewHTML="{Binding PreviewHTML,Mode=TwoWay}"></local:Preview>
        </Grid>

    </Grid>
</Page>
